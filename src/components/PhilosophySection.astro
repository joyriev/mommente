---
import { PHILOSOPHY } from "../constants";
---

<section
  id="philosophy"
  class="py-24 md:py-56 bg-brand-charcoal text-brand-offwhite"
>
  <div class="container mx-auto px-6 md:px-12 max-w-7xl">
    <div class="max-w-4xl mb-32 space-y-10">
      <div class="flex items-center space-x-6">
        <div class="w-12 h-px bg-brand-gold"></div>
        <span
          class="text-xs uppercase tracking-[0.5em] font-bold text-brand-gold"
          >Our Philosophy</span
        >
      </div>
      <h2
        class="text-5xl md:text-[6vw] font-sans font-black uppercase tracking-tighter leading-[0.9]"
      >
        Commerce is <span
          class="italic font-serif font-light lowercase text-brand-gold"
          >shifting.</span
        >
      </h2>
      <p
        class="text-xl md:text-3xl text-brand-offwhite/60 font-light leading-relaxed max-w-3xl"
      >
        People no longer buy just for need — they buy for experience, identity,
        and feeling. We design brands that create small but meaningful moments
        in daily life.
      </p>
    </div>

    <div class="grid md:grid-cols-2 gap-1px bg-white/5 border-y border-white/5">
      {
        PHILOSOPHY.map((item, index) => (
          <div class="bg-brand-charcoal p-12 md:p-20 space-y-8 hover:bg-white/[0.02] transition-colors group">
            <div class="flex items-center space-x-6">
              <span class="text-brand-gold text-5xl font-serif italic opacity-30 group-hover:opacity-100 transition-opacity">
                0{index + 1}
              </span>
              <h3 class="text-2xl font-sans font-black uppercase tracking-tight">
                {item.title}
              </h3>
            </div>
            <p class="text-brand-offwhite/50 text-lg leading-relaxed font-light max-w-md group-hover:text-brand-offwhite/80 transition-colors">
              {item.description}
            </p>
          </div>
        ))
      }
    </div>

    <div
      class="mt-32 pt-12 border-t border-white/5 flex flex-col md:flex-row justify-between items-center gap-12"
    >
      <p
        class="text-lg font-serif italic text-brand-offwhite/40 max-w-lg leading-relaxed"
      >
        "Because sometimes, the difference between ordinary and memorable is
        just a little more thought."
      </p>
      <div
        class="flex items-center space-x-4 text-[10px] uppercase tracking-[0.4em] font-black text-brand-gold"
      >
        <span>Mommente Certified Standard</span>
        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
          <path
            fill-rule="evenodd"
            d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
            clip-rule="evenodd"></path>
        </svg>
      </div>
    </div>
  </div>
</section>
